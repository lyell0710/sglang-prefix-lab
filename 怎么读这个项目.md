# 怎么读这个项目(sglang-prefix-lab · RadixAttention 前缀缓存机理实验室)

> **上游**:跨仓的先后次序见 `Resume/STUDY_MAP.md`;本文只讲**进了这个仓之后怎么读**。
> **同级**:各仓根目录的同名文件结构一致,读熟一个就会读其余五个。

---

## 总纲:这个仓没有 `src/`,读的是实验设计不是实现

其余五个仓读的是"我写的 kernel / 引擎 / 量化算法";**本仓一行推理代码都没写**。
它是拿现成的 SGLang 当被测系统,回答一组机理问题:

> 前缀缓存命中到底省了什么?省的量与什么成正比?什么时候会失效?

所以读法完全不同:**这里的"代码"是 `scripts/` 下的压测与聚合脚本,
读它们是为了学"怎么设计一个能归因的实验",不是学怎么实现功能。**

如果你是为了学 kernel 或引擎实现来的,这个仓给不了;
它给的是另一样同等重要的东西 —— **在一个你改不动的黑盒系统上,
怎么把"它变快了"变成"它因为 X 变快了 Y"**。

第二条:本仓的四条关键发现全部是**带定语的**
(「命中收益正比于被跳过的 prefill,并发将其放大」「上限是 input_len-1 不是 input_len」
「LRU 逐出不是斜坡是悬崖」「调度与路由的『更好』都必须带定语」)。
**定语就是结论本身** —— 读的时候如果把定语略掉,读到的就是错的。

---

## 零、第一次进仓:按顺序打开(75 分钟)

| 步 | 打开什么 | 只看什么 | 时间 |
|---|---|---|---|
| 1 | `README.md` | 「概述」+「性能结果」表 +「关键发现」四条 | 15 min |
| 2 | `docs/theory/01_radix_prefix_cache.md` | 全文(五节速查) | 15 min |
| 3 | `README.md` 的「代码导览」节 | 那段 `bench_prefix.py` 节选 **+ 它下面那段机制要点** | 15 min |
| 4 | `records/EXP-P01_env_single_worker_smoke.md` | §5 结果(看 `cached=1324/1325` 那个数) | 10 min |
| 5 | `records/EXP-P03_hit_benefit_curve.md` | §1 假设 + §5 结果 + §6 分析 | 15 min |
| 6 | 白板自测 | 不看文档,说清「为什么命中上限是 n−1 而不是 n」 | 5 min |

**第 3 步是本仓的钥匙。** 那段代码里藏着整个仓的方法论:
在**同一次流式请求内**既停表(TTFT)又取得 server 侧的 `cached_tokens` ——
"快了多少"与"命中了多少"在同一个响应里闭合。
**归因不依赖推测,是因为两个量在同一条时间线上被同时取到。**

第 6 步那个 `n−1`:调度器把匹配上限压到 `input_len-1`,保证至少重算 1 个 token
以产生 logits。EXP-P01 实测 1324/1325 是它的精确值。
**一个能被精确预言的整数,比任何百分比都更能证明你理解了机制。**

---

## 一、完整阅读顺序(精确到文件)

按**问题递进**排。P01 是地基,P02 是契约,P03 起才是正题。

| # | 问题 | records 入口 | scripts 对应 | 走完能答 |
|---|---|---|---|---|
| 1 | 命中怎么算、上限是多少 | `EXP-P01_env_single_worker_smoke` | `probe_cached.py` | 为什么是 n−1 |
| 2 | 什么会破坏命中(契约) | `EXP-P02_token_contract_matrix` | `contract_matrix.py` | 哪些请求字段会让前缀对不上 |
| 3 | **命中收益曲线** | `EXP-P03_hit_benefit_curve` | `bench_prefix.py` + `aggregate_p03.py` | 收益与什么成正比、并发怎么放大它 |
| 4 | 调度:lpm vs fcfs | `EXP-P04_lpm_vs_fcfs` | `bench_groups.py` + `aggregate_groups.py` | 为什么 lpm 超窗口后 p99 反劣 |
| 5 | **逐出压力** | `EXP-P05_eviction_pressure` | `bench_evict.py` + `aggregate_evict.py` | 为什么 LRU 是悬崖不是斜坡 |
| 6 | 路由与池容量 | `EXP-P06_routing_pool_capacity` | `bench_route_pool.py` + `aggregate_route.py` | 双预测双证伪是怎么做的 |
| 7 | 8B 规模复现 | `EXP-P07` / `EXP-P08` | 同上 | 结论在更大模型上是否成立 |
| — | 早期收编 | `EXP-S00` / `EXP-S01` | — | (历史,router 矩阵的预注册协议) |

配套讲义:`docs/lectures/01_radix_attention_full.md`(机理)与
`02_scheduling_routing_boundaries.md`(调度与路由的边界)。

### 每个实验的固定四步(30-40 分钟)

| 步 | 动作 |
|---|---|
| 1 | 读 `records/EXP-PNN` 的 **§1 目的与假设** —— 本仓的假设写得最像样,都是可证伪的 |
| 2 | 读对应 `scripts/bench_*.py` 的**对照臂怎么设的**(哪些变量固定、哪个在扫) |
| 3 | 读 `scripts/aggregate_*.py` —— **聚合口径比压测本身更容易出错** |
| 4 | 回 records 的 §6 分析与 **§7 异常与开放问题** |

### 两个里程碑

- 走完 **#3**:能讲清前缀缓存的收益模型,并给出一条自家实测曲线。
- 走完 **#5**:能讲清缓存失效的机制(重用距离 vs 池容量),这是最难也最值钱的一段。

---

## 二、文档分层:按目的进

| 你的目的 | 进哪一层 | 时间 |
|---|---|---|
| 知道有什么、结论是什么 | `README.md` 概述 + 性能结果 + 关键发现四条 | 15 min |
| 某个机制的一页纸速查 | `docs/theory/01`~`03`(五节格式) | 10 min/篇 |
| 机制要真的学会 | `docs/lectures/` 的**对应小节**(两篇) | 30-60 min |
| 查数字怎么来的、可不可信 | `records/EXP-PNN` 的 §2 / §5 / **§7** | 10 min |
| 练面试怎么讲 | `docs/talk/prefix_lab_walkthrough.md` + `whiteboard_card_reuse_distance.md` | 20 min |
| 服务怎么起、端口怎么配 | `scripts/svc.sh` + `config/*.json` + `ENV.md` | 按需 |
| 压测工具本身的口径 | `docs/tooling/bench_serving_v0.5.18.md` | 按需 |
| 简历证据 | `RESUME_EVIDENCE.md` | — |
| 未执行的下一阶段 | `docs/PLAN_router_matrix.md`(S02-S07 预注册协议) | — |

**本仓特有的一条**:`docs/tooling/bench_serving_v0.5.18.md` 记的是**上游压测工具的口径**
(它怎么计时、怎么统计)。**读任何一份性能记录之前先读它** ——
不知道工具怎么算 TTFT,就无法判断记录里的数字意味着什么。

---

## 三、代码怎么看:读实验设计,不是读实现

### 读法一 · 先找对照臂,再看被测臂

打开任何一个 `scripts/bench_*.py`,第一件事不是看它怎么发请求,
是问:**这个实验的对照臂是什么?哪些变量被固定住了?**

例:`bench_groups.py`(P04,lpm vs fcfs)—— 两条臂唯一的差别是调度策略,
其余(模型、并发、请求序列、seed)必须逐位相同。
**看不出对照臂在哪的脚本,读了也学不到东西。**

### 读法二 · 聚合脚本比压测脚本更容易出错,要单独读

`aggregate_*.py` 决定了原始日志怎么变成表里的数字:
取 mean 还是 p99、去不去 warmup、异常值怎么处理。
**压测跑对了但聚合口径错了,得到的是一个很稳定的错误数**(这类错误最难发现)。

本仓每个实验都是 `bench_*.py` + `aggregate_*.py` 成对的,**成对读**。

### 读法三 · 服务型实验先读 preflight

`scripts/preflight.sh` 是每次跑之前的硬 gate(检查 GPU 是否被别的进程占用、
端口是否冲突)。这不是样板 —— 它是一次真实事故的产物(与另一个 agent 抢卡,
造成 OOM 假象)。**读它就知道服务型实验的坑在哪。**

同理 `scripts/provenance.sh`:每次跑记录环境与版本,否则跨天的数字不可比。

### 读法四 · 日志是一等公民

`runtime/` 下是 router 与 worker 的实际日志(`router.log`、`w0.log`)。
本仓的很多结论是从日志里读出来的(逐 token 的 device_hit 计数闭环)。
**在这个仓里,"看日志"不是 debug 手段,是测量手段。**

---

## 四、自测验收

不看文档,在白纸上做:

| 里程碑 | 验收动作 |
|---|---|
| #1-#2 后 | 说清命中上限为什么是 n−1;列出三种会让前缀对不上的请求差异 |
| #3 后 | 画出命中收益随命中长度/并发的变化趋势,说清"正比于被跳过的 prefill"是什么意思 |
| #5 后 | 讲清"命中 ⇔ 池容量 ≥ 重用距离"这条判据,并解释为什么是悬崖不是斜坡 |
| #6 后 | 讲一遍双预测双证伪:两个预测分别是什么、各被什么数据推翻 |

面试前一晚:`docs/talk/prefix_lab_walkthrough.md` + 重用距离白板卡 +
README 关键发现四条(**连定语一起背**)。

---

## 五、当前缺口

1. **两篇讲义正在被工具链加目录**(`docs/lectures/*.md` 有未提交改动),读前先确认状态。
2. **S02-S07 未执行**:`docs/PLAN_router_matrix.md` 里的 router 策略矩阵是
   预注册协议,不是已完成的实验。引用时不要当成结论。
3. 跨机复现需先看 `ENV.md`(端口、venv、模型路径都是本机约定)。
4. `EXP-S00/S01` 是并仓时收编的历史记录,口径与 P 系列不完全一致,只作史料。
